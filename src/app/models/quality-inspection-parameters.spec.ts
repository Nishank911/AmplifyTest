import { QualityInspectionParameters } from './quality-inspection-parameters';

describe('QualityInspectionParameters', () => {
  it('should create an instance', () => {
    expect(new QualityInspectionParameters()).toBeTruthy();
  });
});
